import SwiftUI

struct LoadingView: View {

    var body: some View {
        VStack{
            Text("Loading")
                .font(.caption)
                .italic()
            Text("📡")
                .font(.title)
        }
    }

}
